export type Registration = {
  name: string;
  phone: string;
  password: string;
};

export type User = {
  id: number;
  name: string;
  phone: string;
  password: string;
};

export type Authorization = {
  name: string;
  password: string;
};
